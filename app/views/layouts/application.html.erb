<!DOCTYPE html>
<html>
  <head>
    <script src="js/bootstrap.min.js"></script>

    <title>Pitt Make New 2019 Christmas Challenge</title>
    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  
    <link href="https://fonts.googleapis.com/css?family=Quicksand:300,400,700&display=swap" rel="stylesheet">  </head>
    <script src="https://kit.fontawesome.com/26fce218a0.js" crossorigin="anonymous"></script>
  <body>
  <nav class="navbar navbar-default navbar-fixed-top topnav" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to image_tag("http://duke-makenew.northcarolina.wpengine.com/wp-content/uploads/sites/4/2017/08/makenew-logo-final-long-M.png", :size => "165x25"), root_path, class: "navbar-brand" %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <%= link_to "Challenges", challenges_path %>
                </li>
                <li>
                    <%= link_to "Completed", completed_path %>
                </li>
                <li>
                    <%= link_to "Verses", verses_path %>
                </li>
            </ul>
        </div>
    <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>
    <div class="flash">
      <% flash.each do |message_type, message| %>
        <% if message_type == "success" %>
          <div class="alert alert-success alert-dismissible" role="alert">
          <%= message %>
          </div>
        <% else %>
          <div class="alert alert-danger alert-dismissible" role="alert">
            Error(s) with your submission: <%= message %>
          </div>
        <% end %>
      <% end %>
    </div>
    <%= yield %>

    <!-- Modal -->
    <div class="modal fade" id="logChallenge" tabindex="-1" role="dialog" aria-labelledby="logChallengeTitle" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h3 class="modal-title" id="logChallengeTitle">Log a challenge</h3>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <%= form_for @comment, :html => {:role => 'form'} do |f| %>
            <div class="modal-body">
              <div class="row">
                <div class="col-lg-12">
                  <%= render 'shared/error_messages' %>
                  <div class="form-group">
                    <%= f.label :name, "Name (first and last)" %>
                    <%= f.text_field :name, { :class => "form-control"} %>
                  </div>
    
                  <div class="form-group">
                    <%= f.label :team %>
                    <%= f.select :team, options_for_select(["Bros", "Sis", "Staff"]), {:prompt => 'Select Team'}, { :class => "form-control" } %>
                  </div>
                  <div class="form-group">
                    <%= f.label :content, "Blurb" %>
                    <%= f.text_area :content, { :class => "form-control", rows: "5"} %>
                  </div>

                  <div class="form-group">
                    <%= f.label :mission_id, "Challenge" %>
                    <select class="form-control" name="comment[mission_id]" id="comment_mission_id">
                      <option value>Select Challenge</option>
                      <optgroup label="Spiritual Growth">Spiritual Growth
                        <% @sg.each do |mission| %>
                          <option value=<%= mission.id %>><%= mission.name %> - <%= mission.points%> points</option>
                        <% end %>
                      </optgroup>
                      <optgroup label="Evangelism">Evangelism
                        <% @evangelism.each do |mission| %>
                          <option value=<%= mission.id %>><%= mission.name %> - <%= mission.points%> points</option>
                        <% end %>
                      </optgroup>
                      <optgroup label="Service/Gratitude">Service/Gratitude
                        <% @ser.each do |mission| %>
                          <option value=<%= mission.id %>><%= mission.name %> - <%= mission.points%> points</option>
                        <% end %>
                      </optgroup>
                      <optgroup label="Equipping">Equipping
                        <% @eq.each do |mission| %>
                          <option value=<%= mission.id %>><%= mission.name %> - <%= mission.points%> points</option>
                        <% end %>
                      </optgroup>
                    </select>
                  </div>

                  <div class="form-group">
                    <%= f.label :avatar, "Upload a Picture (optional, but recommended)" %>
                    <%= f.file_field :avatar, { :class => "form-control"} %>
                  </div>

                  <div class="form-group">
                    <%= f.label :video, "Upload a Video (optional)" %>
                    <%= f.file_field :video, { :class => "form-control"} %>
                  </div>
                </div>
              </div>  
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
              <%= f.submit "Submit my post", class: "btn btn-info" %>
            </div>
          <% end %>
        </div> 
      </div>
    </div>
  </body>
</html>
